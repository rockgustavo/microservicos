spring:
  application:
    name: "Eureka Server"
  security:
    basic:
      enabled: true
    user:
      name: ms-microservicos-eureka
      password: adm123

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
